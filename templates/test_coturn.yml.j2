package:
  coturn:
    installed: true
port:
{% if coturn_tls %}
  tcp:{{ coturn_tls_listening_port }}:
    listening: true
{% endif %}
  udp:{{ coturn_listening_port }}:
    listening: true
service:
  coturn:
    enabled: true
    running: true
user:
  turnserver:
    exists: true
    groups:
    - turnserver
    shell: /bin/false
group:
  turnserver:
    exists: true
process:
  turnserver:
    running: true
